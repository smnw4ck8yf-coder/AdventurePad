# Historical creator-kit material

This directory contains internal package-authoring material from an older creator workflow. It is retained for history only.

Current community skins are authored as one 4720×4040 PNG with 21 registered regions. See `../README.md`; creators do not write `skin.json` or build `.apskin` archives.
